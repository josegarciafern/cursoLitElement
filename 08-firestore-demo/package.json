{
  "scripts": {
    "build": "rimraf dist && rollup -c rollup.config.js",
    "start:build": "npm run build && web-dev-server --root-dir dist --app-index index.html --open --compatibility none",
    "start": "web-dev-server"
  },
  "devDependencies": {
    "@open-wc/building-rollup": "^1.9.4",
    "@web/dev-server": "^0.1.8",
    "deepmerge": "^4.2.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.40.0"
  },
  "name": "firestore-demo",
  "version": "0.0.0",
  "description": "Webcomponent firestore-demo following open-wc recommendations",
  "author": "firestore-demo",
  "license": "MIT",
  "dependencies": {
    "@dile/dile-button": "github:Polydile/dile-button",
    "dile-input": "^3.1.2",
    "firebase": "^8.2.9",
    "lit-element": "^2.4.0",
    "lit-html": "^1.3.0"
  }
}
